/**
 * Sum two numbers.
 *
 * This function sums together two numbers. This description can span
 * multiple lines.
 *
 * And paragraphs!
 * @param a First thing to sum
 * @param b Second thing to sum
 */
int sum(int a, int b);
