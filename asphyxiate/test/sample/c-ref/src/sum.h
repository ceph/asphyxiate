/** @file */
/* doxygen will ignore cross-references unless you're inside a
   @defgroup or have used @file. */

/**
 * Sum two numbers.
 *
 * This function sums together two numbers. As in sum(). Duh.
 */
int sum(int a, int b);

/**
 * See sum().
 */
int subtract(int a, int b);
